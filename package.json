{
  "name": "afterbuy",
  "version": "0.1.0",
  "private": true,
  "description": "AfterBuy — Target.com price-drop monitoring (domain contracts)",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "migrate:up": "tsx src/db/cli.ts up",
    "migrate:down": "tsx src/db/cli.ts down",
    "serpapi:live-audit": "tsx src/serpapi/run-live-audit.ts"
  },
  "engines": {
    "node": ">=22.5"
  },
  "dependencies": {
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "vitest": "^3.0.9"
  }
}
